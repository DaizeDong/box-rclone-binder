# box-binder - NEVER commit secret material. Pointers only live in machines.yaml.
secrets/
*.conf
rclone.conf
config.json
*.env
secrets.env
runtime.env
*.pem
*.key
*.p12
*.pfx
credentials*
*.token
access.json
state.json
.box-binder-state*
# the live inventory may name internal hosts; commit machines.example.yaml only
machines.yaml

# self-evolve run artifacts (sandboxes, events, holdout) - never commit
.sie/

# python
__pycache__/
*.pyc
.pytest_cache/
.coverage
htmlcov/

# os / editor
.DS_Store
Thumbs.db
*.swp
